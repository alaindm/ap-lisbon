apps:
  - script   : bin/www
    instances: 1
    exec_mode: cluster
    # autorestart: false
    # watch  : true
    env    :
      NODE_ENV: production
    env_production:
      NODE_ENV: production
